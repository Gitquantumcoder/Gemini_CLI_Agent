[project]
name = "pikachu"
version = "0.1.0"
description = "My first agent using gemini api"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "google-genai>=2.8.0",
    "python-dotenv>=1.2.2",
]
